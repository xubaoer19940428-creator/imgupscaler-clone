import type { Metadata } from 'next'
import App from '../../src/App'

export const metadata: Metadata = { title: 'Reimagine AI — Img.Upscaler' }

export default function ReimaginePage() {
  return <App />
}
